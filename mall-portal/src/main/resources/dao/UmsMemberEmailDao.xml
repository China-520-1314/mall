<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.portal.dao.UmsMemberEmailDao">
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*) FROM ums_member WHERE email = #{email}
    </select>

    <select id="selectMemberIdByEmail" resultType="long">
        SELECT id FROM ums_member WHERE email = #{email} LIMIT 1
    </select>

    <update id="updateEmail">
        UPDATE ums_member SET email = #{email} WHERE id = #{memberId}
    </update>
</mapper>
